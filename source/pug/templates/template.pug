include ./include

block vars
    - var pageTitle = 'Title'
    - var main = false

doctype html
html(lang="ru-RU")
    block head
        +head()

    body
        block icon
        block header
            +header({})
        block content
        block footer
            +footer({})
        block scripts
            +scripts()
